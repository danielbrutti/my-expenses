import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mateam.myexpenses',
  appName: 'myExpenses',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
